query GetMovie($slug: String!) {
  movie(slug: $slug) {
    id
    description
    director
    duration
    posterUrl
    rating
    releasedOn
    title
    totalGross
    genres {
      name
    }
  }
}
